<types>
  <type name="SVGOptions" desc="An option for creating an svg.">
    <prop number name="width">
      The width of the `svg`.
    </prop>
    <prop number name="height">
      The height of the `svg`.
    </prop>
    <prop string name="content">
      The content to put inside of the `svg`.
    </prop>
    <prop boolean name="stretch" default="true">
      Expand the `svg` to the width of the container by not setting `width` and `height` attributes.
    </prop>
  </type>
</types>